# Contentful 配置
# 你的 Space ID
NEXT_PUBLIC_CONTENTFUL_SPACE_ID=your_space_id
# 你的 Delivery Token (只读)
NEXT_PUBLIC_CONTENTFUL_DELIVERY_TOKEN=your_delivery_token
# 博客文章的 Contentful 模型 ID
NEXT_PUBLIC_CONTENTFUL_BLOG_MODEL=blogPost
# 每页显示的博客文章数量
NEXT_PUBLIC_BLOG_ITEMS_PER_PAGE=10
# 是否使用数据库作为博客内容数据源（true 使用 PostgreSQL，false 使用本地 Markdown）
NEXT_PUBLIC_USE_DB_CONTENT=false
# 博客文章排序方式
# 仅在使用 Contentful CMS 时生效。
# 可用选项示例：
# -sys.createdAt      : 按创建时间降序（默认，最新在前）
# sys.createdAt       : 按创建时间升序
# -sys.updatedAt      : 按更新时间降序
# sys.updatedAt       : 按更新时间升序
# fields.title        : 按标题字母顺序
# -fields.title       : 按标题字母倒序
NEXT_PUBLIC_CONTENTFUL_BLOG_ORDER_TYPE=-sys.createdAt

# 国际化配置
NEXT_PUBLIC_I18N=true

# 备案信息
NEXT_PUBLIC_ICP_CODE=ICP备XXXXXX号
NEXT_PUBLIC_POLICE_LICENSE=浙公网安备 3XXXXXX号

# 站点配置
# 站点 URL，用于生成固定链接
NEXT_PUBLIC_SITE_URL=https://yospace.waveyo.cn
# 站点描述
# 站点标题 (浏览器标签页标题)
NEXT_PUBLIC_SITE_TITLE=Weilai
NEXT_PUBLIC_SITE_TITLE_EN=Weilai
# 导航栏标题 (左上角 Logo 文字)
NEXT_PUBLIC_NAV_TITLE=未来
NEXT_PUBLIC_NAV_TITLE_EN=Future

# 版权信息
# 说明：
# - NEXT_PUBLIC_SITE_NAME 控制版权尾部站点名（示例：WaveYo）
# - NEXT_PUBLIC_SITE_START_YEAR 控制起始年份（示例：2024）
# - 当前年份由浏览器运行时获取（不依赖 build 结果）
# - 未配置起始年份，或起始年份 >= 当前年份时，仅显示当前年份（示例：© 2026 WaveYo）
# - 起始年份 < 当前年份时，显示年份跨度（示例：© 2024 - 2026 WaveYo）
NEXT_PUBLIC_SITE_NAME=WaveYo
NEXT_PUBLIC_SITE_START_YEAR=2024
# 站点介绍
NEXT_PUBLIC_SITE_DESCRIPTION=未来是未知的旅程，愿每一步前行，都有爱陪伴左右。
NEXT_PUBLIC_SITE_DESCRIPTION_EN=The future is an unknown journey, may every step forward be accompanied by love.
# 个人名称 (多个名称用逗号分隔)
NEXT_PUBLIC_PROFILE_NAMES=未来°,这是未来呐,这是未来呐
NEXT_PUBLIC_PROFILE_NAMES_EN=Weilai,Future,Future
# Favicon URL (相对路径如 '/favicon.ico' 或绝对 URL 如 'https://example.com/icon.png')
NEXT_PUBLIC_FAVICON_URL=/weilai.png
# 个人头像 URL
NEXT_PUBLIC_PROFILE_IMAGE=https://cravatar.com/avatar/23c51fce715e221bb371b4bd2437d8bc?s=512

# Blog 跳转配置
# mode: 'external' (外部跳转) | 'internal' (默认，内部路由)
NEXT_PUBLIC_BLOG_MODE=external
# 外部博客地址 (仅当 mode 为 external 时生效)
NEXT_PUBLIC_BLOG_URL=https://your-external-blog.com

# Blog 内容模型与展示配置
# 分类展示总开关，缺省视为 true
NEXT_PUBLIC_BLOG_CATEGORY_ENABLED=true
# 标签展示总开关，缺省视为 true
NEXT_PUBLIC_BLOG_TAGS_ENABLED=true
# 系列展示总开关，缺省视为 true
NEXT_PUBLIC_BLOG_SERIES_ENABLED=true
# 推荐阅读模块开关，缺省视为 true
NEXT_PUBLIC_BLOG_RECOMMEND_ENABLED=true
# 无分类文章的默认分类 ID，留空表示不强制归类
NEXT_PUBLIC_BLOG_CATEGORY_FALLBACK_ID=
# 分类展示名策略：i18n-first | frontmatter-first，缺省为 i18n-first
NEXT_PUBLIC_BLOG_CATEGORY_LABEL_STRATEGY=i18n-first
# 列表卡片中可见标签最大数量，缺省为 3
NEXT_PUBLIC_BLOG_TAGS_MAX_VISIBLE=3
# 系列排序模式：by-index | by-date，缺省为 by-index
NEXT_PUBLIC_BLOG_SERIES_SORT_MODE=by-index
# 分类徽标样式：pill | tag | dot，缺省为 pill
NEXT_PUBLIC_BLOG_CATEGORY_BADGE_VARIANT=pill
# 分类徽标位置：above-title | inline-title | hidden，缺省为 above-title
NEXT_PUBLIC_BLOG_CATEGORY_POSITION=above-title
# 置顶文章列表展示样式：separate-section | icon-only | badge，缺省为 separate-section
NEXT_PUBLIC_BLOG_PINNED_STYLE=separate-section
# 详情页相关列表最大文章数量，缺省为 50
NEXT_PUBLIC_BLOG_RELATED_LIMIT=50

# Music API 配置
NEXT_PUBLIC_MUSIC_API_BASE=https://netmusic.waveyo.cn/
NEXT_PUBLIC_MUSIC_PLAYLIST_ID=12752948320

# 数据库配置（PostgreSQL）
DATABASE_URL=postgresql://user:password@host:5432/database_name?sslmode=require
# 可选：控制 SSL 模式，常见取值为 disable、require 等，具体取决于数据库提供商要求
PGSSLMODE=require

# Redis 配置（预留，后续 Phase 使用）
REDIS_URL=redis://default:password@host:6379

# Admin 后台配置
# 后台入口路径前缀，默认为 /admin，可根据需要修改为自定义路径（例如 /dashboard）
NEXT_PUBLIC_ADMIN_PATH=/admin
# 单管理员后台用户名，仅作为示例，请在实际部署时根据需要修改
ADMIN_USERNAME=admin
# 单管理员后台密码示例，请在实际部署时改为强密码，且不要提交到版本控制
ADMIN_PASSWORD=please_change_this_password
# 管理后台 JWT 对称加密密钥，需使用足够随机的长字符串，建议仅在服务端环境变量中配置
ADMIN_JWT_SECRET=please_replace_with_a_long_random_secret_string
